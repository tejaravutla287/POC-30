apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitops-helm-sync
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/tejaravutla287/POC-30.git'
    targetRevision: HEAD
    path: app-chart        # ArgoCD auto-detects Chart.yaml here and triggers Helm
  destination:
    server: 'https://default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
