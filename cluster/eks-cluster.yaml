apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: gitops-poc-cluster
  region: us-east-1
  version: "1.30"
managedNodeGroups:
  - name: gitops-workers
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 1
    maxSize: 3
    volumeSize: 20
